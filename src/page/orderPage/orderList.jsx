const OrderList = ({ ingredientsList = [] }) => {
  console.log(ingredientsList);
  return (
    <section>
      <div>
        <h1>Your Price</h1>
      </div>
    </section>
  );
};

export default OrderList;
